#pragma once
#include "ContainerManager.h"


//might not even need to be no more :/
extern int buffer_size;
extern ContainerManager<std::string> g_cBuffer;
extern ContainerManager<std::string> g_cBuffer_last;